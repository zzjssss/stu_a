"""
第 00 节 · 学习路径总览 —— 函数 / 闭包 / 装饰器工厂
======================================================

一、为什么要学这三样
------------------------------------------------------
函数  : 代码复用的最小单元，一切的基础。
闭包  : "函数 + 它记住的外层变量"。它是装饰器的底层原理，
        也是工厂函数、缓存、限流器、回调的实现手段。
装饰器: 闭包的一个"官方语法糖应用"。Web 框架(Flask/FastAPI)、
        任务队列(Celery)、ORM(Django)全靠它做路由注册、权限、日志、缓存。
装饰器工厂: 带参数的装饰器 @retry(times=3)，是三层嵌套结构，
        真实项目里 90% 的装饰器都是这种形态。

结论：这四者是一条链，不是四个独立知识点。
      函数 -> 函数是一等公民 -> 闭包 -> 装饰器 -> 装饰器工厂


二、学习路线（对应本目录下的文件，建议按顺序）
------------------------------------------------------
01_函数基础.py
    · def 与调用、函数是对象
    · 5 种参数形态：位置 / 关键字 / 默认 / *args / **kwargs
    · 仅位置(/) 与 仅关键字(*) 分隔符
    · 可变默认参数陷阱、返回值与多返回值
    · 类型注解 + docstring 的工程写法
    ★ 实战片段：接口请求参数解析与校验函数

02_作用域与一等公民.py
    · LEGB 查找规则、global 与 nonlocal
    · 函数对象的内部属性(__defaults__ / __code__ / __kwdefaults__)
    · lambda 的能力与边界
    · 高阶函数：map / filter / sorted(key=) / any / all / reduce
    ★ 实战片段：ORM 风格的数据排序、过滤与聚合

03_闭包.py
    · 自由变量、__closure__ 与 cell 对象
    · 闭包三要素、闭包的生命周期
    · 两大陷阱：循环延迟绑定、修改外层不可变变量
    · 闭包 = 没有 self 的轻量对象
    ★ 实战片段：接口限流器 / 简易缓存 / 数据库连接工厂

04_装饰器基础.py
    · @ 语法糖的等价展开
    · 通用签名 wrapper(*args, **kwargs)
    · functools.wraps 为什么必须加
    · 多装饰器叠加的"洋葱模型"与执行顺序
    · 注册型装饰器（不改变行为，只登记）
    ★ 实战片段：耗时统计、登录校验、Flask 路由注册原理

05_装饰器工厂.py
    · 三层结构：工厂 -> 装饰器 -> wrapper
    · @retry(times=3, delay=1) 失败重试
    · 兼容"带括号/不带括号"两种写法
    · 用类实现装饰器（__init__ + __call__）
    · 装饰器作用于类、装饰器叠加类的属性
    · functools 全家桶：lru_cache / partial / singledispatch
    ★ 实战片段：可配级别日志、TTL 缓存、超时控制、幂等锁

06_综合实战_迷你Web框架.py
    · 把上面所有知识揉进一个能跑的小框架
    · 路由注册 + 中间件链 + 权限 + 计时 + 缓存 + 参数校验
    ★ 这就是 Flask / FastAPI 装饰器的最小可运行缩影

07_练习与自检.py
    · 12 道由易到难的练习（先自己写，再看底部参考答案）


三、每节的固定结构
------------------------------------------------------
每个文件都是「可直接运行」的：
    小节讲解 demo_x_y()   ->  简单例子 + 逐行注释
    实战片段 practice_*() ->  真实项目里会遇到的代码 + 讲解
    if __name__ == "__main__":  ->  按顺序跑完所有 demo

跑法：
    PyCharm 里右键 Run
    或命令行： python 01_函数基础.py


四、学习方法建议（很重要）
------------------------------------------------------
1. 先跑再改：每个 demo 先运行看输出，再改一个参数看输出怎么变。
2. 手写展开：学装饰器时，把 @deco 手动改写成 f = deco(f)，
   写三遍，你就再也不会被"多层嵌套"绕晕。
3. 画调用栈：闭包/装饰器出错时，用纸画出"谁持有谁的引用"。
4. 用 dis / __closure__ 看真相：不要猜，打印出来看。
5. 每节做完做 07 的对应练习，能默写才算过。


五、自检清单（学完打勾）
------------------------------------------------------
函数：
    [ ] 说得清 *args 和 **kwargs 分别收集什么
    [ ] 知道 def f(x, lst=[]) 为什么是坑，会写正确版本
    [ ] 能解释 def f(a, /, b, *, c) 三种参数各怎么传
    [ ] 会给函数写完整类型注解和 docstring

作用域：
    [ ] 能背出 LEGB 并解释每一层是什么
    [ ] 知道 nonlocal 和 global 的区别及使用场景
    [ ] 能解释循环里 lambda 延迟绑定的原因与两种修复方式

闭包：
    [ ] 能说出闭包成立的三个条件
    [ ] 会用 __closure__ 和 cell_contents 查看被捕获的变量
    [ ] 能独立写出一个带状态的计数器/限流器闭包

装饰器：
    [ ] 能把 @deco 展开成等价的普通代码
    [ ] 知道不写 functools.wraps 会丢失什么
    [ ] 能说出两层装饰器叠加后，注册顺序与执行顺序的差别

装饰器工厂：
    [ ] 能默写三层结构，并说清每层的参数是谁
    [ ] 能写出一个 @retry(times=3, exceptions=(ValueError,))
    [ ] 能用类实现装饰器，并解释它和函数式的取舍
    [ ] 知道什么时候该用 functools.lru_cache 而不是自己写


六、预计投入
------------------------------------------------------
01 + 02 : 1 ~ 1.5 天（多半是复习）
03      : 1 天（闭包是分水岭，务必吃透）
04      : 1 天
05      : 1.5 天（本套的重点与难点）
06 + 07 : 1 天（综合与巩固）
合计约 5 ~ 6 天，每天 2 小时左右。
"""


def main() -> None:
    """打印学习路径清单，作为开始学习的入口。"""
    files: list[str] = [
        "01_函数基础.py            -> 参数形态 / 注解 / 陷阱",
        "02_作用域与一等公民.py    -> LEGB / lambda / 高阶函数",
        "03_闭包.py                -> 自由变量 / 陷阱 / 限流器",
        "04_装饰器基础.py          -> @ 语法糖 / wraps / 洋葱模型",
        "05_装饰器工厂.py          -> 三层结构 / retry / 类装饰器",
        "06_综合实战_迷你Web框架.py -> 全部知识的综合应用",
        "07_练习与自检.py          -> 12 道练习 + 参考答案",
    ]
    print("=" * 60)
    print("Python 函数 / 闭包 / 装饰器工厂  学习路径")
    print("=" * 60)
    for i, line in enumerate(files, start=1):
        print(f"{i}. {line}")
    print("=" * 60)
    print("建议：打开 01_函数基础.py，边跑边读注释。")


if __name__ == "__main__":
    main()
